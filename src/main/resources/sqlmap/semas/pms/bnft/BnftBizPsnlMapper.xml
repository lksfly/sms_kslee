<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 수혜기업개인관계 -->
<mapper namespace="kr.bizdata.semas.pms.bnft.mapper.BnftBizPsnlMapper">
	
	<insert id="insert">
		/* SQL ID : BnftBizPsnlMapper.insert */
		INSERT INTO BNFT_BIZ_PSNL (
		       DI_VAL           /* DI값 */
		     , BRNO             /* 사업자등록번호 */
		) VALUES (
		       #{diVal}         /* DI값 */
		     , #{brno}          /* 사업자등록번호 */
		)
	</insert>
	
	<select id="exists" resultType="boolean">
		/* SQL ID : BnftBizPsnlMapper.exists */
		SELECT CASE WHEN COUNT(*) > 0 THEN '1' ELSE '0' END
		  FROM BNFT_BIZ_PSNL
		 WHERE DI_VAL = #{diVal}  /* DI값 */
		   AND BRNO = #{brno}     /* 사업자등록번호 */
	</select>
	
</mapper>
